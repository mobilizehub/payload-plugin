---
'@mobilizehub/payload-plugin': patch
---

Strip example code to create base plugin structure. Removes boilerplate dashboard components, custom endpoints, and plugin collection. Simplifies plugin to minimal configuration ready for actual implementation.
